#pragma once

#include <vector>

// output stream object
class Processor {
    std::vector<double> buf;
    size_t to_process = 0;

public:
    Process

public:


    // writing to the stream
    Processor& operator<<(Processor& stream, std::vector<double> in) {
        for (auto e : )
        return stream;
    }

private:

};