#pragma once

#include <vector>
#include <complex>

std::vector<std::complex<double>> hann_window_lookup(int n) {
    // (-cos(angle) + 1)/2
    double phi;
    std::vector<std::complex<double>>
    for (int i = 0; i < n; ++i) {

    }
    // such that angle goes up 2pi in N samples
}